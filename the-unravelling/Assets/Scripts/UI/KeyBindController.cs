using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class KeyBindController : MonoBehaviour {
    
    // TODO: Look into how this is done with the new input system. It should be persistent and not let you rebind the same key several times.
    // There seems to be a alright tutorial on how to do this: https://www.youtube.com/watch?v=csqVa2Vimao
    // Unity's input system has a little sample thing you can install that contains a re-binding script as shown in the video.
    // Another tutorial that goes over the same https://www.youtube.com/watch?v=dUCcZrPhwSo, shorter video.

    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
